import { HomeContentDirective } from './home-content.directive';

describe('HomeContentDirective', () => {
  it('should create an instance', () => {
    const directive = new HomeContentDirective();
    expect(directive).toBeTruthy();
  });
});
